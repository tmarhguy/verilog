module baud_gen (
    input  logic clk,
    input  logic rst_n,
    output logic tick_o // Generates a single cycle pulse at baud rate (or Nx baud for RX)
);
    // TODO: Counter implementation
endmodule
